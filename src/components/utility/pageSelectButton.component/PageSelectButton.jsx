import "./pageSelectButton.scss";

import { Link } from "react-router-dom";

const PageSelectButton = ({ page }) => {
    return (
        <p>hi</p>
        // <Link className="page-select-button" to={`/animelist/${page}`}>
        //     {page}
        // </Link>
    );
};

export default PageSelectButton;
